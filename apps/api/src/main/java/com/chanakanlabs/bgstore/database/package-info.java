/**
 * jOOQ table types generated from the Flyway migrations by the {@code jooqCodegen} task.
 *
 * <p>Only this package-info is written by hand; everything else in the package is generated into
 * {@code build/generated/jooq} and must not be edited. Open, like the HTTP contract, because every
 * module that owns tables reads its own tables from here.
 */
@org.springframework.modulith.ApplicationModule(
    displayName = "Database schema",
    type = org.springframework.modulith.ApplicationModule.Type.OPEN)
package com.chanakanlabs.bgstore.database;
