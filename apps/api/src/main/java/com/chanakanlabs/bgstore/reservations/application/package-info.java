/**
 * Application commands, queries, and inbound boundaries for staff reservation operations.
 *
 * <p>Add the application service here only after the OpenAPI contract and domain invariants are
 * agreed. That service must apply the identity module's staff-or-manager access policy.
 */
package com.chanakanlabs.bgstore.reservations.application;
