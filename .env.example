POSTGRES_DB=bgstore
POSTGRES_USER=bgstore
POSTGRES_PASSWORD=bgstore-local-only
REDIS_PASSWORD=bgstore-local-only
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin-local-only
KEYCLOAK_CLIENT_SECRET=bgstore-local-client-secret
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Set both when something else already owns host port 8081 (the browser-facing
# Keycloak port); Compose derives KC_HOSTNAME from KEYCLOAK_PUBLIC_URL.
#KEYCLOAK_HOST_PORT=18081
#KEYCLOAK_PUBLIC_URL=http://localhost:18081
