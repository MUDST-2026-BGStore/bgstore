#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$repo_root"
[[ -f .env ]] || cp -- .env.example .env
[[ -f .env.local ]] || cp -- .env.example .env.local

"$HOME/.local/bin/mise" reshim >/dev/null
amp orb services ensure >/dev/null
